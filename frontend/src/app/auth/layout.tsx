/**
 * Auth layout — no sidebar, just a clean centered layout.
 * Used for the OAuth callback page and any future auth pages.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
